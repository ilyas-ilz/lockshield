import { ArticlePageSkeleton } from "@/components/frontend/skeletons";

/** Long-form article: hero, cover, prose column, related sidebar. */
export default function BlogPostLoading() {
  return <ArticlePageSkeleton />;
}
